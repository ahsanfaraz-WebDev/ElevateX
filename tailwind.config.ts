import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: ["class"],
  content: [
    "./pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
    "./sanity/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      screens: {
        xs: "475px",
      },
      colors: {
        primary: {
          "100": "#D6EAF8", // Light Soft Blue
          DEFAULT: "#2471A3", // Deep Ocean Blue
        },
        secondary: "#F39C12", // Warm Golden Yellow
        black: {
          "100": "#2C3E50", // Dark Blue-Gray
          "200": "#1C2833", // Deep Charcoal
          "300": "#95A5A6", // Muted Gray
          DEFAULT: "#000000", // Pure Black
        },
        white: {
          "100": "#ECF0F1", // Soft Cool White
          DEFAULT: "#FFFFFF", // Pure White
        },
      },
      fontFamily: {
        "work-sans": ["var(--font-work-sans)"],
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
      boxShadow: {
        100: "2px 2px 0px 0px rgb(0, 0, 0)",
        200: "2px 2px 0px 2px rgb(0, 0, 0)",
        300: "2px 2px 0px 2px #2471A3",
      },
    },
  },
  // eslint-disable-next-line @typescript-eslint/no-require-imports
  plugins: [require("tailwindcss-animate"), require("@tailwindcss/typography")],
};

export default config;
